const { Client } = require('pg'); const client = new Client({ user: 'erp_admin', host: '195.35.22.13', database: 'shastika_erp', password: 'Shastika2026', port: 5432 }); client.connect().then(() => client.query('SELECT * FROM invoice_items WHERE invoice_id = $1', ['b3636d0e-a698-49d9-ab47-82037370694c'])).then(res => { console.log(res.rows); client.end(); }).catch(err => { console.log(err); client.end(); });
